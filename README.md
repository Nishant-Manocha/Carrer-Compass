# Carrer-Compass

## Run frontend + backend together

From the project root:

```bash
npm install
npm run install:all
npm run dev
```

### Notes
- Frontend runs in `frontened/`
- Backend runs in `backend/`

